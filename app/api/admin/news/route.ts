import { NextResponse } from "next/server";
import { cookies } from "next/headers";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
function makeSlug(title:string){const base=title.toLowerCase().trim().replace(/[^\p{L}\p{N}]+/gu,"-").replace(/^-+|-+$/g,"");return `${base||"news"}-${Date.now()}`;}
async function admin(){const c=await cookies();const s=c.get("admin_session");if(!s?.value)return false;const u=await db.user.findUnique({where:{id:s.value},select:{role:true}});return !!u&&u.role==="ADMIN";}
export async function GET(){try{if(!(await admin()))return NextResponse.json({success:false,message:"Unauthorized."},{status:401});const news=await db.news.findMany({orderBy:{createdAt:"desc"},include:{category:true},take:100});return NextResponse.json({success:true,news});}catch(error){console.error("News fetch failed:",error);return NextResponse.json({success:false,message:"Unable to fetch news."},{status:500});}}
export async function POST(request:Request){try{if(!(await admin()))return NextResponse.json({success:false,message:"Unauthorized."},{status:401});const body=await request.json();const title=typeof body.title==="string"?body.title.trim():"";const description=typeof body.description==="string"?body.description.trim():"";const content=typeof body.content==="string"?body.content.trim():"";const imageUrl=typeof body.imageUrl==="string"?body.imageUrl.trim():"";const sourceName=typeof body.sourceName==="string"?body.sourceName.trim():"";const sourceUrl=typeof body.sourceUrl==="string"?body.sourceUrl.trim():"";const externalId=typeof body.externalId==="string"?body.externalId.trim():"";const categoryId=typeof body.categoryId==="string"?body.categoryId:"";const language=body.language==="EN"?"EN":"HI";const status=body.status==="PUBLISHED"?"PUBLISHED":body.status==="ARCHIVED"?"ARCHIVED":"DRAFT";const isFeatured=body.isFeatured===true;const isBreaking=body.isBreaking===true;if(!title||!categoryId)return NextResponse.json({success:false,message:"Title and category are required."},{status:400});const category=await db.category.findUnique({where:{id:categoryId}});if(!category)return NextResponse.json({success:false,message:"Category not found."},{status:400});const finalExternalId=externalId||`manual-${Date.now()}`;const existingNews=await db.news.findUnique({where:{externalId:finalExternalId}});if(existingNews)return NextResponse.json({success:false,message:"This news already exists."},{status:409});const news=await db.news.create({data:{title,slug:makeSlug(title),description:description||null,content:content||null,imageUrl:imageUrl||null,sourceName:sourceName||null,sourceUrl:sourceUrl||null,externalId:finalExternalId,language,status,isFeatured,isBreaking,publishedAt:status==="PUBLISHED"?new Date():null,categoryId},include:{category:true}});return NextResponse.json({success:true,message:"News created successfully.",news});}catch(error){console.error("News creation failed:",error);return NextResponse.json({success:false,message:"Unable to create news."},{status:500});}}
